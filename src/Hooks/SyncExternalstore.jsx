import React from 'react'
import { useSyncExternalStore } from 'react'

const SyncExternalstore = () => {
    /**
     * useSyncExternalStore is used for subscribing or linking external non-react state store to react.
     * It ensures that your component stays in sync with the external store,
     * providing a consistent way to read and subscribe to changes.
     * most likely used with state management libraries like Redux or Zustand.
    */

  return (
    <div>
      
    </div>
  )
}

export default SyncExternalstore

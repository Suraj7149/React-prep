import React, {useMemo} from 'react'

const MemoExp = () => {

    /**
     * useMemo is on of the 2 hooks created to increase the performace of React applications.
     * It memoizes the result of a function and recomputes it only when its dependencies change.
     * 
     * This is useful for optimizing expensive calculations that don't need to be re-executed on every render.
     */
  return (
    <div>
      
    </div>
  )
}

export default MemoExp
